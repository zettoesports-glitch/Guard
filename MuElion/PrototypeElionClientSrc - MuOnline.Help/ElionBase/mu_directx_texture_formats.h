#ifndef __MU_DIRECTX_TEXTURE_FORMATS_H__
#define __MU_DIRECTX_TEXTURE_FORMATS_H__

#pragma once

static const DXGI_FORMAT DirectXFormats[ETextureFormatMax] =
{
	DXGI_FORMAT_R8_UNORM,
	DXGI_FORMAT_R8_SNORM,
	DXGI_FORMAT_R8_UINT,
	DXGI_FORMAT_R8_SINT,

	DXGI_FORMAT_R8G8_UNORM,
	DXGI_FORMAT_R8G8_SNORM,
	DXGI_FORMAT_R8G8_UINT,
	DXGI_FORMAT_R8G8_SINT,

	DXGI_FORMAT_R8G8B8A8_UNORM,
	DXGI_FORMAT_R8G8B8A8_SNORM,
	DXGI_FORMAT_R8G8B8A8_UINT,
	DXGI_FORMAT_R8G8B8A8_SINT,

	DXGI_FORMAT_R16_UNORM,
	DXGI_FORMAT_R16_SNORM,
	DXGI_FORMAT_R16_UINT,
	DXGI_FORMAT_R16_SINT,
	DXGI_FORMAT_R16_FLOAT,

	DXGI_FORMAT_R16G16_UNORM,
	DXGI_FORMAT_R16G16_SNORM,
	DXGI_FORMAT_R16G16_UINT,
	DXGI_FORMAT_R16G16_SINT,
	DXGI_FORMAT_R16G16_FLOAT,

	DXGI_FORMAT_R16G16B16A16_UNORM,
	DXGI_FORMAT_R16G16B16A16_SNORM,
	DXGI_FORMAT_R16G16B16A16_UINT,
	DXGI_FORMAT_R16G16B16A16_SINT,
	DXGI_FORMAT_R16G16B16A16_FLOAT,

	DXGI_FORMAT_R32_UINT,
	DXGI_FORMAT_R32_SINT,
	DXGI_FORMAT_R32_FLOAT,

	DXGI_FORMAT_R32G32_UINT,
	DXGI_FORMAT_R32G32_SINT,
	DXGI_FORMAT_R32G32_FLOAT,

	DXGI_FORMAT_R32G32B32_UINT,
	DXGI_FORMAT_R32G32B32_SINT,
	DXGI_FORMAT_R32G32B32_FLOAT,

	DXGI_FORMAT_R32G32B32A32_UINT,
	DXGI_FORMAT_R32G32B32A32_SINT,
	DXGI_FORMAT_R32G32B32A32_FLOAT,

	DXGI_FORMAT_D16_UNORM,
	DXGI_FORMAT_D32_FLOAT,
};

static const DXGI_FORMAT DirectXFormatsTypeless[ETextureFormatMax] =
{
	DXGI_FORMAT_R8_UNORM,
	DXGI_FORMAT_R8_SNORM,
	DXGI_FORMAT_R8_UINT,
	DXGI_FORMAT_R8_SINT,

	DXGI_FORMAT_R8G8_UNORM,
	DXGI_FORMAT_R8G8_SNORM,
	DXGI_FORMAT_R8G8_UINT,
	DXGI_FORMAT_R8G8_SINT,

	DXGI_FORMAT_R8G8B8A8_UNORM,
	DXGI_FORMAT_R8G8B8A8_SNORM,
	DXGI_FORMAT_R8G8B8A8_UINT,
	DXGI_FORMAT_R8G8B8A8_SINT,

	DXGI_FORMAT_R16_UNORM,
	DXGI_FORMAT_R16_SNORM,
	DXGI_FORMAT_R16_UINT,
	DXGI_FORMAT_R16_SINT,
	DXGI_FORMAT_R16_FLOAT,

	DXGI_FORMAT_R16G16_UNORM,
	DXGI_FORMAT_R16G16_SNORM,
	DXGI_FORMAT_R16G16_UINT,
	DXGI_FORMAT_R16G16_SINT,
	DXGI_FORMAT_R16G16_FLOAT,

	DXGI_FORMAT_R16G16B16A16_UNORM,
	DXGI_FORMAT_R16G16B16A16_SNORM,
	DXGI_FORMAT_R16G16B16A16_UINT,
	DXGI_FORMAT_R16G16B16A16_SINT,
	DXGI_FORMAT_R16G16B16A16_FLOAT,

	DXGI_FORMAT_R32_UINT,
	DXGI_FORMAT_R32_SINT,
	DXGI_FORMAT_R32_FLOAT,

	DXGI_FORMAT_R32G32_UINT,
	DXGI_FORMAT_R32G32_SINT,
	DXGI_FORMAT_R32G32_FLOAT,

	DXGI_FORMAT_R32G32B32_UINT,
	DXGI_FORMAT_R32G32B32_SINT,
	DXGI_FORMAT_R32G32B32_FLOAT,

	DXGI_FORMAT_R32G32B32A32_UINT,
	DXGI_FORMAT_R32G32B32A32_SINT,
	DXGI_FORMAT_R32G32B32A32_FLOAT,

	DXGI_FORMAT_R16_TYPELESS,
	DXGI_FORMAT_R32_TYPELESS,
};

static const DXGI_FORMAT DirectXFormatsDirect[ETextureFormatMax] =
{
	DXGI_FORMAT_R8_UNORM,
	DXGI_FORMAT_R8_SNORM,
	DXGI_FORMAT_R8_UINT,
	DXGI_FORMAT_R8_SINT,

	DXGI_FORMAT_R8G8_UNORM,
	DXGI_FORMAT_R8G8_SNORM,
	DXGI_FORMAT_R8G8_UINT,
	DXGI_FORMAT_R8G8_SINT,

	DXGI_FORMAT_R8G8B8A8_UNORM,
	DXGI_FORMAT_R8G8B8A8_SNORM,
	DXGI_FORMAT_R8G8B8A8_UINT,
	DXGI_FORMAT_R8G8B8A8_SINT,

	DXGI_FORMAT_R16_UNORM,
	DXGI_FORMAT_R16_SNORM,
	DXGI_FORMAT_R16_UINT,
	DXGI_FORMAT_R16_SINT,
	DXGI_FORMAT_R16_FLOAT,

	DXGI_FORMAT_R16G16_UNORM,
	DXGI_FORMAT_R16G16_SNORM,
	DXGI_FORMAT_R16G16_UINT,
	DXGI_FORMAT_R16G16_SINT,
	DXGI_FORMAT_R16G16_FLOAT,

	DXGI_FORMAT_R16G16B16A16_UNORM,
	DXGI_FORMAT_R16G16B16A16_SNORM,
	DXGI_FORMAT_R16G16B16A16_UINT,
	DXGI_FORMAT_R16G16B16A16_SINT,
	DXGI_FORMAT_R16G16B16A16_FLOAT,

	DXGI_FORMAT_R32_UINT,
	DXGI_FORMAT_R32_SINT,
	DXGI_FORMAT_R32_FLOAT,

	DXGI_FORMAT_R32G32_UINT,
	DXGI_FORMAT_R32G32_SINT,
	DXGI_FORMAT_R32G32_FLOAT,

	DXGI_FORMAT_R32G32B32_UINT,
	DXGI_FORMAT_R32G32B32_SINT,
	DXGI_FORMAT_R32G32B32_FLOAT,

	DXGI_FORMAT_R32G32B32A32_UINT,
	DXGI_FORMAT_R32G32B32A32_SINT,
	DXGI_FORMAT_R32G32B32A32_FLOAT,

	DXGI_FORMAT_R16_UNORM,
	DXGI_FORMAT_R32_FLOAT,
};

static const mu_size DirectXFormatSizes[ETextureFormatMax] =
{
	sizeof(mu_uint8) * 1,
	sizeof(mu_uint8) * 1,
	sizeof(mu_uint8) * 1,
	sizeof(mu_uint8) * 1,

	sizeof(mu_uint8) * 2,
	sizeof(mu_uint8) * 2,
	sizeof(mu_uint8) * 2,
	sizeof(mu_uint8) * 2,

	sizeof(mu_uint8) * 4,
	sizeof(mu_uint8) * 4,
	sizeof(mu_uint8) * 4,
	sizeof(mu_uint8) * 4,

	sizeof(mu_uint16) * 1,
	sizeof(mu_uint16) * 1,
	sizeof(mu_uint16) * 1,
	sizeof(mu_uint16) * 1,
	sizeof(mu_uint16) * 1,

	sizeof(mu_uint16) * 2,
	sizeof(mu_uint16) * 2,
	sizeof(mu_uint16) * 2,
	sizeof(mu_uint16) * 2,
	sizeof(mu_uint16) * 2,

	sizeof(mu_uint16) * 4,
	sizeof(mu_uint16) * 4,
	sizeof(mu_uint16) * 4,
	sizeof(mu_uint16) * 4,
	sizeof(mu_uint16) * 4,

	sizeof(mu_uint32) * 1,
	sizeof(mu_uint32) * 1,
	sizeof(mu_float) * 1,

	sizeof(mu_uint32) * 2,
	sizeof(mu_uint32) * 2,
	sizeof(mu_float) * 2,

	sizeof(mu_uint32) * 3,
	sizeof(mu_uint32) * 3,
	sizeof(mu_float) * 3,

	sizeof(mu_uint32) * 4,
	sizeof(mu_uint32) * 4,
	sizeof(mu_float) * 4,

	sizeof(mu_uint16) * 1,
	sizeof(mu_float) * 1,
};

static const D3D11_USAGE DirectXUsages[ETextureUsageMax] =
{
	D3D11_USAGE_DEFAULT,
	D3D11_USAGE_DYNAMIC,
	D3D11_USAGE_DYNAMIC,
};

#endif